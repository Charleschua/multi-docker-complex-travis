sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t charles/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true charles/react-test npm test

after_success:
  - docker build -t charles/multi-client ./client
  - docker build -t charles/multi-nginx ./nginx
  - docker build -t charles/multi-server ./server
  - docker build -t charles/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push charles/multi-client
  - docker push charles/multi-nginx
  - docker push charles/multi-server
  - docker push charles/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'ap-southeast-1'
  app: 'multi-docker'
  env: 'Multi-docker-env'
  bucket_name: 'elasticbeanstalk-ap-southeast-1-339713084658'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
